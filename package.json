{
  "name": "halflife",
  "version": "1.0.0",
  "description": "GitHub Action: dependency decay checker",
  "private": true,
  "scripts": {
    "build": "ncc build src/index.ts -o dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "overrides": {
    "undici": "^6.24.0"
  },
  "_overrides_notes": {
    "undici": "Pinned to ^6.24.0 to patch CVEs in the 5.x line (CVE-2024-24758, CVE-2024-30260, CVE-2024-30261) pulled in transitively"
  }
}
